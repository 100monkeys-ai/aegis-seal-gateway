# SPDX-License-Identifier: AGPL-3.0-only
# Copyright 2026 100monkeys.ai

name: Release

on:
  push:
    tags:
      - '*.*.*'  # Matches 0.1.0, 0.2.0-pre-alpha, etc.
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., 0.1.0)'
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: aegis-seal-gateway
  FORCE_PRERELEASE: 'true'  # TODO: set to 'false' or remove after alpha

permissions:
  contents: write

jobs:
  build-linux:
    name: Build Linux x86_64
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: aegis-seal-gateway

    steps:
      - name: Checkout gateway
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          path: aegis-seal-gateway

      - name: Checkout proto repo
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          repository: 100monkeys-ai/aegis-proto
          path: aegis-proto

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master @ 2026-09-03; toolchain pinned by rust-toolchain.toml and the input below
        with:
          toolchain: "1.98.1"
          targets: x86_64-unknown-linux-gnu

      - name: Cache cargo registry & build
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2

      - name: Build release binary
        run: |
          cargo build --release --locked

      - name: Create tarball
        run: |
          cd target/release
          tar -czf aegis-seal-gateway-linux-x86_64.tar.gz aegis-seal-gateway
          cd ../..
          mv target/release/aegis-seal-gateway-linux-x86_64.tar.gz .

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: aegis-seal-gateway-linux-x86_64
          path: aegis-seal-gateway/aegis-seal-gateway-linux-x86_64.tar.gz
          if-no-files-found: error
          retention-days: 1

  build-macos:
    name: Build macOS (Apple Silicon)
    runs-on: macos-latest
    defaults:
      run:
        working-directory: aegis-seal-gateway

    steps:
      - name: Checkout gateway
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          path: aegis-seal-gateway

      - name: Checkout proto repo
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          repository: 100monkeys-ai/aegis-proto
          path: aegis-proto

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # master @ 2026-09-03; toolchain pinned by rust-toolchain.toml and the input below
        with:
          toolchain: "1.98.1"
          targets: aarch64-apple-darwin

      - name: Cache cargo registry & build
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2

      - name: Build release binary
        run: |
          cargo build --release --locked --target aarch64-apple-darwin

      - name: Create tarball
        run: |
          cd target/aarch64-apple-darwin/release
          tar -czf aegis-seal-gateway-macos-aarch64.tar.gz aegis-seal-gateway
          cd ../../..
          mv target/aarch64-apple-darwin/release/aegis-seal-gateway-macos-aarch64.tar.gz .

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: aegis-seal-gateway-macos-aarch64
          path: aegis-seal-gateway/aegis-seal-gateway-macos-aarch64.tar.gz
          if-no-files-found: error
          retention-days: 1

  create-release:
    name: Create GitHub Release
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts

      - name: Display structure
        run: ls -R artifacts

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "VERSION=${{ inputs.version }}" >> $GITHUB_OUTPUT
            echo "IS_PRERELEASE=false" >> $GITHUB_OUTPUT
          elif [ "${{ github.ref_type }}" = "tag" ]; then
            echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
            echo "IS_PRERELEASE=false" >> $GITHUB_OUTPUT
          else
            SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
            VERSION="v0.0.0-main.${SHORT_SHA}"
            echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
            echo "IS_PRERELEASE=true" >> $GITHUB_OUTPUT
          fi

      - name: Generate checksums
        run: |
          cd artifacts
          for dir in */; do
            cd "$dir"
            for file in *.tar.gz; do
              sha256sum "$file" > "$file.sha256"
            done
            cd ..
          done
          cd ..

      - name: Create Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ steps.version.outputs.VERSION }}
          name: AEGIS SEAL Gateway ${{ steps.version.outputs.VERSION }}
          draft: false
          prerelease: ${{ env.FORCE_PRERELEASE == 'true' || steps.version.outputs.IS_PRERELEASE == 'true' }}
          generate_release_notes: true
          files: |
            artifacts/aegis-seal-gateway-linux-x86_64/aegis-seal-gateway-linux-x86_64.tar.gz
            artifacts/aegis-seal-gateway-linux-x86_64/aegis-seal-gateway-linux-x86_64.tar.gz.sha256
            artifacts/aegis-seal-gateway-macos-aarch64/aegis-seal-gateway-macos-aarch64.tar.gz
            artifacts/aegis-seal-gateway-macos-aarch64/aegis-seal-gateway-macos-aarch64.tar.gz.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
